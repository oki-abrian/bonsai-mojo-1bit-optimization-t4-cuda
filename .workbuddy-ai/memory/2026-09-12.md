# 2026-09-12

## Alat detokenisasi log inferensi (baru)

**Masalah:** `main.mojo` hanya mencetak `>> [GEN] token id: N | X ms`, tanpa teks.
Output jadi tampak "ngaco" (mis. `248068, 271, 248069`) padahal itu tag normal.
Selain itu tidak ada logika berhenti di EOS — grep `eos|stop|break` di `main.mojo` = 0 hasil.

**Dibuat:**
- `detok_infer.py` — baca log Kaggle (array JSON `{stream_name,data}` atau teks biasa),
  pecah per-run pada `>> [MOJO-NATIVE]`, ambil `[GEN] token id`, decode via
  `tokenizers.Tokenizer`. Memotong di EOS pertama (248046 `<|im_end|>`).
  Auto-unduh `tokenizer.json` dari dataset Kaggle `okiabrian/bonsai-27b-mlx-1bit`
  bila belum ada; cache di `~/.cache/bonsai_tok/tokenizer.json`.
- `cek_infer.sh` — wrapper: siapkan venv+`tokenizers`, opsi `--push` (jalankan
  `push_to_kaggle.sh` dulu), lalu panggil `detok_infer.py`.

**Fakta tokenizer Bonsai-27B (Qwen3.5, vocab 248320):** special token di 248044+,
BUKAN 151xxx ala Qwen standar.
`248044=<|endoftext|>`, `248045=<|im_start|>`, `248046=<|im_end|>` (=eos),
`248056=<|image_pad|>`, `248057=<|video_pad|>`, `248068=<think>`, `248069=</think>`.
`eos_token_id=248046`, `bos_token_id=248044` (dari `config.json`).

**Hasil cek log run 2026-09-12 (build_log.json, exit 0, semua gate KHQ lolos):**
- Run 1/4 (prompt "Hello", 24 tok): `<think>\n\n</think>\n\nHello! How can I help you today?`
  → **BENAR, tidak ngaco.** EOS di token ke-14.
- Run 2 (prompt panjang, 8 tok): `Here's thinking process:\n\n1.`
- Run 3 (BONSAI_NO_FUSE=1): `<think>` lalu `<|im_end|>` x23 → **degenerate**,
  divergen dari baseline di token ke-2 (no-fuse TIDAK bit-exact di sini).
- Run 5 (512 tok): loop berulang "Analyze the User's Request" — kualitas turun di
  konteks panjang (dugaan artefak kuantisasi 1-bit), tapi token stream bit-exact vs KHQ.

**Temuan bug lain (belum diperbaiki):**
1. `src/tokenizer.py` masih hardcode special id Qwen lama
   (`EOS=151643`, `IM_START=151644`, `IM_END=151645`). Untuk model ini harusnya
   248046/248045. Hanya dipakai `generate.py` (CLI Python), bukan `main.mojo`.
2. `deploy_on_kaggle.sh` ~baris 657: `PT = int(sys.argv[2].split(",")[0])`
   mengira elemen pertama daftar token = panjang prompt. Padahal `parse_int_list`
   di `main.mojo` memperlakukan input sebagai daftar id murni (panjang dihitung
   internal ke slot 0). Jadi angka "prompt N token" di ringkasan A/B salah
   (itu id token pertama). Hanya laporan, tidak memengaruhi inferensi.
3. `push_to_kaggle.sh` menulis artefak ke `.../fix/dist_kaggle_mojo` (sibling),
   bukan `bonsai-1bit-t4-mojo/dist_kaggle_mojo` — .whl di dalam project itu basi (4 Sep).

## Kernel terpisah: TES INFERENSI SOAL SUSAH

Pipeline lengkap (`push_to_kaggle.sh` -> `deploy_on_kaggle.sh`) selalu memakai
prompt "Hello" (9 token) + run panjang hardcode, dan `--max-tokens 24` di Run 1 —
tidak cukup untuk soal sulit. Env var TIDAK menembus ke dalam kernel Kaggle, jadi
prompt harus dibawa sebagai file di dalam kernel.

**Kernel baru (terpisah, tidak menyentuh pipeline lama):**
- `infer_susah/kernel-metadata.json` — id `okiabrian/bonsai-infer-susah`, GPU T4,
  `code_file: run_infer.py`. `dataset_sources`: bonsai-mojo-source + bonsai-27b-mlx-1bit.
  `kernel_sources`: `okiabrian/bonsai-mojo-t4-build` (untuk ambil cache build).
- `infer_susah/run_infer.py` — entrypoint: cari sumber di /kaggle/input (syarat
  `deploy_on_kaggle.sh` + `main.mojo`, meniru `run_deploy.py`), ekstrak .tar,
  ambil `mojo_build_cache.tar.gz` dari output kernel lama, tulis
  `/kaggle/working/infer_config.json`, jalankan `deploy_infer.sh`.
- `infer_susah/deploy_infer.sh` — hanya: restore cache -> pixi -> nvcc .so sm_75 ->
  `pixi run mojo build -I . main.mojo` -> encode prompt (apply_chat_template) ->
  `bonsai_infer --max-tokens N --gpu`. TANPA self-test/KHQ/A-B (jauh lebih cepat).
- `infer_susah/infer_config.json` — prompt + max_tokens. Ini cara prompt masuk ke
  kernel (file ikut terunggah), bukan env var.
- `tes_infer_susah.sh` — orkestrator lokal: `--prompt`, `--prompt-file`,
  `--max-tokens`, `--no-push`, `--status`. Push kernel -> poll status -> unduh log
  via endpoint legacy `/api/v1/kernels/output` (Bearer token) -> detok jadi teks.
  Output ke `../dist_kaggle_mojo/infer_susah/`.

**Prompt default (soal sulit, jawaban terverifikasi):** kolam diisi pipa A 6 jam,
pipa B 4 jam; keduanya 2 jam, lalu hanya A. Jawaban benar = **3 jam**
(2 jam -> 5/6 kolam; sisa 1/6 dengan laju 1/6 per jam = 1 jam).

Catatan: `kaggle datasets files` terpaginasi, dan `-r tar` pada `datasets version`
membuat sebagian direktori jadi `.tar` — karena itu pencarian sumber wajib
mengandalkan `main.mojo` + `deploy_on_kaggle.sh` di top level, bukan keberadaan `src/`.

### PELAJARAN PENTING: kernel "script" Kaggle TIDAK membawa file pendamping

Percobaan pertama gagal: `[ERROR] deploy_infer.sh tidak ditemukan`, padahal
`deploy_infer.sh` + `infer_config.json` ada di direktori yang di-push. Kaggle
membungkus `code_file` jadi notebook (`__script__.ipynb`) dan hanya itu yang
tersedia saat runtime.

**Solusi:** `run_infer.py` dibuat MANDIRI. Ia adalah template dengan penanda
`# ==BEGIN PROMPT== / MAXTOK / DEPLOY ==`. `tes_infer_susah.sh` me-render
`infer_susah/_push/run_infer.py` dengan cara menyuntikkan `repr()` dari
prompt (dari `infer_config.json`) dan seluruh isi `deploy_infer.sh`, lalu
mem-push direktori `_push/`. Jadi skrip shell ditanam sebagai string di Python.

### HASIL TES SOAL SUSAH (kernel v2, COMPLETE, 6m51s, T4)

Prompt (81 token): soal kolam — pipa A 6 jam, pipa B 4 jam, keduanya 2 jam lalu
hanya A. Jawaban benar = 3 jam. `max_tokens=512`, greedy.

**Hasil: MODEL DEGENERASI (repetition loop).**
- 512 token ter-generate, hanya **33 id unik**.
- 42 token pertama masih koheren: `Here's a thinking process:\n\n1.  **Analyze the user's request:**\n    *   **Goal:** Calculate the total time required to fill the pool completely.\n    *   **Input`
- Mulai token ke-43 masuk siklus 7 token yang berulang **67 kali**:
  `\n        *   **Context:**`
- Tidak pernah memunculkan EOS, tidak pernah sampai ke jawaban.
- TOP2 batas prefill: 8160 (19.3125) | 1596 (16.328125). ~54,4 ms/token (~18,4 tok/s).

**Kontras:** prompt sederhana ("Hello") menghasilkan jawaban BENAR
(`Hello! How can I help you today?`). Jadi jalur ini benar untuk keluaran pendek,
tapi runtuh pada generasi panjang.

**Dugaan penyebab (belum diuji):**
1. **Greedy/argmax** — decoding greedy memang rawan repetition loop; referensi
   MLX kemungkinan memakai sampling. `main.mojo` hanya greedy, tanpa
   temperature/top-p/repetition-penalty.
2. Drift state rekurensi GDN (fp16) pada langkah panjang.
3. Degradasi kuantisasi 1-bit itu sendiri.

Langkah berikutnya yang paling murah: tambah sampling (temperature/top-p) atau
repetition penalty di `main.mojo`, lalu ulangi tes dengan prompt yang sama.

**Cara pakai ulang:** `./tes_infer_susah.sh` (atau `--prompt "..."`,
`--max-tokens N`, `--no-push`, `--status`). Output log ke
`../dist_kaggle_mojo/infer_susah/`.

## SAMPLING DITAMBAHKAN KE main.mojo — hasil tes A/B

**Perubahan `main.mojo`** (semua OPT-IN, default greedy supaya kontrak bit-exact
gate KHQ tidak berubah):
- Fungsi baru: `env_int()`, `rng_next()` (xorshift64), `sample_from_logits()`.
- Env: `BONSAI_TEMP_X100` (0 = mati/greedy), `BONSAI_REP_PENALTY_X100`,
  `BONSAI_REP_WINDOW`, `BONSAI_LOGIT_FLOOR_X10`, `BONSAI_SEED`.
- "logit floor" menggantikan top-k/top-p TANPA sorting (token >floor di bawah
  maksimum dimatikan) — jauh lebih murah di CPU.
- Logits fp16 (V=248320 ≈ 0,5 MB) ditarik D2H lalu disampling di CPU; biaya
  terukur +3,1 ms/token (54,4 -> 57,5 ms/token).
- **Jebakan scope:** `main.mojo` punya DUA blok `if use_gpu_matmul():` terpisah
  (setup buffer vs loop decode). `h_log` lokal di blok pertama TIDAK terlihat di
  blok kedua — harus di-hoist (`var logits_buf = ...` di scope fungsi, diisi di
  blok pertama), sama seperti pola `h_token_out_holder`.
- Catatan literal: `var vmax = -3.0e38` tanpa konteks jadi Float64 -> error
  `__gt__` dua dtype. Pakai `Float32(-3.0e38)`.

**GATE KOMPILASI LOKAL (penting):** build di Mac SELALU berakhir
`Metal Compiler failed to compile metallib` — itu codegen Apple GPU, terjadi
SETELAH analisis semantik. Sudah diuji dengan menyisipkan error tipe sengaja:
error tipe tetap muncul lebih dulu. Jadi compile lokal tetap gate yang sah dan
hemat (2-15 detik) sebelum membakar ~6 menit run Kaggle.
Perintah: `MODULAR_HOME="$PWD/.pixi/envs/default/share/max" ./.pixi/envs/default/bin/mojo build -I . main.mojo -o /tmp/check`
Target Linux (`--target-triple x86_64-unknown-linux-gnu`) TIDAK menolong — Metal
tetap jalan.

**Jebakan plumbing:** `run_infer.py` awalnya hanya menulis `prompt` + `max_tokens`
ke `/kaggle/working/infer_config.json`, sehingga parameter sampling tidak pernah
sampai ke `deploy_infer.sh` (terbaca default 0 -> greedy). Run pertama dengan
sampling menghasilkan output IDENTIK dengan greedy. Fix: penanda `==BEGIN CFGJSON==`
di template, diisi `json.dumps(cfg)` oleh `tes_infer_susah.sh`.

**HASIL A/B (prompt kolam sama, 512 token, T4):**

| | Greedy | Sampling T=0.70, rep 1.10 |
|---|---|---|
| id unik | 33 / 512 | 108 / 512 |
| siklus kaku | 7 token x 67 ulangan | **TIDAK ADA** |
| ms/token | 54,4 | 57,5 |
| isi | `\n *   **Context:**` berulang | incoherent, campur bahasa |

**KESIMPULAN PENTING:** sampling **menghilangkan loop kaku**, tapi keluarannya
TETAP tidak koheren (mengulang angka, campur Indonesia/Inggris, muncul
`means "Jamin"` / `means "tuna"`, `</think>` dua kali). Jadi degenerasi BUKAN
sekadar artefak greedy — ada masalah lebih dalam (model 1-bit / kernel).
Prompt `Hello` tetap benar, jadi jalur ini benar untuk keluaran pendek saja.

**Antrean berikutnya:** uji hipotesis kernel vs kuantisasi — mis. bandingkan
logits di batas prefill pada prompt sulit vs referensi MLX, atau uji prompt
panjang tapi non-reasoning untuk memisahkan "reasoning rusak" dari "konteks
panjang rusak".




---

## Dukungan MULTI-PROMPT + tes pemisah (lanjutan)

**Tujuan:** memisahkan "penalaran rusak" dari "konteks panjang rusak". Karena
kernel "script" Kaggle hanya membawa `code_file`, semua dijalankan dalam SATU
kernel: 3 prompt berurutan, tiap prompt di proses `bonsai_infer` terpisah
(cache KV bersih; bobot dimuat ulang tiap iterasi).

**Perubahan file:**
- `infer_susah/deploy_infer.sh` — blok ekor diganti jadi loop `while` atas
  `prompt_list.json`. Normalizer menerima `prompt` tunggal ATAU `prompts` (list
  dict/str). Tiap iterasi: cetak penanda `>> [RUN] idx=N max_tokens=M` dan
  `>> [PROMPT-JSON] "<json>"`, encode via `AutoTokenizer.apply_chat_template`
  (sama seperti `deploy_on_kaggle.sh`), lalu jalankan `bonsai_infer`.
  **Catatan bug yang diperbaiki:** edisi setengah jalan sempat menyisakan
  `$PROMPT_TOKENS`/`$MAXNEW` yang sudah tidak terdefinisi -> `deploy_infer.sh`
  rusak. Sudah selesai.
- `infer_susah/run_infer.py` — penanda baru `==BEGIN PROMPTSJSON==`;
  `write_config()` menulis `cfg["prompts"]` bila diisi, else `cfg["prompt"]`.
- `tes_infer_susah.sh` — flag `--separator-test` (tulis 3 prompt ke config);
  render `PROMPTSJSON`; ringkasan config menampilkan daftar prompt;
  `--prompt` otomatis membatalkan mode multi-prompt (`pop("prompts")`).
- `detok_infer.py` — `split_runs()` kini membaca `>> [PROMPT-JSON]` (JSON, aman
  untuk teks multi-baris/berkutip) dan `>> [RUN] idx/max_tokens`. Penanda dicetak
  SEBELUM `>> [MOJO-NATIVE]` run tsb, jadi dibaca dari blok `prev`.
  Header run menampilkan `RUN n (prompt #k)`.
- `infer_susah/infer_config.json` — default kini = 3 prompt tes pemisah.

**Verifikasi (semua lolos):** `bash -n` + `py_compile`; render offline -> 17150
byte, `mode MULTI (3 prompt)`; `write_config()` -> 3 entri; loop diuji dengan
stub `transformers` + stub `bonsai_infer` -> log 3 run, detok mengenali
`prompt #1/#2/#3` dan `max 96/512/512`.

**Isi tes pemisah:** (1) 5 nama buah tropis [max 96] — sanity koherensi;
(2) cerita 250 kata [max 512] — panjang TANPA penalaran; (3) soal kolam
[512] — penalaran. Tafsir: bila #1/#2 bagus tapi #3 rusak -> penalaran;
bila #2 juga rusak -> generasi panjang/konteks.

**Perintah:** `./tes_infer_susah.sh --no-sync` (hanya wrapper yang berubah,
`main.mojo` tak berubah) atau `./tes_infer_susah.sh --separator-test`.

---

## Riset pengaturan resmi Bonsai 27B 1-bit

Ditulis ke `NOTES_MODEL_BONSAI_SETTINGS.md` (mengikuti konvensi `NOTES_*.md`).
Sumber: docs.prismml.com (models/bonsai-27b, run/llamacpp, run/server) + kartu
model `prism-ml/Bonsai-27B-gguf` dan `prism-ml/Bonsai-27B-mlx-1bit`.

**KOREKSI PENTING:** basis model adalah **Qwen3.6-27B**, BUKAN Qwen3.5. Lisensi
Apache 2.0, konteks 262.144 token.

**Fakta kunci yang mengubah arah diagnosis:**
- Sampling resmi: **temp 0,5–0,7 · top-p 0,85–0,95 · top-k 20 · min-p 0**.
  Dokumentasi PrismML punya DUA versi (benchmark 0,7/0,95 vs llama.cpp
  0,5/0,85) dan memuat TODO internal soal ini. **Tidak ada repetition penalty
  yang dispesifikasi.**
- Proyek kita **belum mengimplementasikan top-k/top-p/min-p sama sekali**.
  Greedy = argmax murni; "sampling" kita pakai `logit_floor` 10,0 yang jauh
  lebih longgar. Kandidat penyebab terkuat untuk loop kaku.
- 27B **thinking ON secara default**. Kendalinya: `thinking_budget_tokens`
  (0=mati, −1=tanpa batas), `--reasoning-budget N`, `BONSAI_THINKING=0`,
  atau `enable_thinking: false` di chat template. Kita: ON tanpa batas, dipotong
  `--max-tokens` -> ini sebab run #1 (daftar buah) tak pernah menyebut buah.
- Kuantisasi native: **"Binary g128"**, bobot {−1,+1}, group 128, skala FP16
  per grup, **1,125 bpw**, tanpa bias. MLX menyimpan skala+bias -> 1,25 bpw
  (ini "Affine Bonsai" kita).
- Arsitektur: 64 blok, ~75% linear / 25% full attention, **16 dari 64** lapisan
  punya cache attention penuh, **KV cache 4-bit** (bagian desain, bukan opsi),
  DSpark speculative decoding (aktif di CUDA).
- **1-bit `Q1_0` sudah di-merge ke llama.cpp upstream** -> ada implementasi CUDA
  referensi yang tervalidasi. Pembanding pamungkas: jalankan llama.cpp CUDA pada
  prompt identik, bandingkan urutan token dengan port Mojo kita.
- Kualitas publikasi (mode thinking, 15 benchmark): 1-bit = **89,5%** dari FP16;
  matematika **91,66**, coding 81,88. Ternary 94,6%.

**Kesimpulan:** degenerasi kita **bukan** kelemahan 1-bit — jarak ke angka
publikasi terlalu jauh. Urutan perbaikan: (1) implementasikan top-k/top-p/min-p
resmi, (2) tambah stop di EOS, (3) kendalikan thinking, (4) baru A/B jalur kernel,
(5) bandingkan dengan llama.cpp CUDA.

---

## Implementasi sampler resmi Bonsai (top-k / top-p / min-p)

Perintah pengguna: "pakai thinking aja, kerjakan, jangan asumsi jika tidak diminta".
-> thinking tetap ON (tidak pakai `enable_thinking=False`), dan yang
diimplementasikan HANYA yang ada di spesifikasi resmi.

**`main.mojo` — `sample_from_logits` ditulis ulang:**
- Urutan mengikuti rantai sampler llama.cpp: **top-k -> top-p -> min-p ->
  temperature**. top-k/top-p/min-p bekerja pada logits **MENTAH**; temperature
  diterapkan paling akhir tepat sebelum pengundian.
- top-k tanpa sorting penuh: daftar kecil (K <= 256) yang selalu urut menurun,
  sisipkan hanya bila kandidat lolos ambang `topk_val[K-1]` -> O(V) konstanta kecil.
- top-p = prefix terkecil dengan kumulatif >= p; min-p = prefix yang lolos
  `p[j] >= min_p * p[0]` (tanpa pembagian, karena sudah urut menurun).
- Buffer baru: `samp_topk_val`, `samp_topk_p` (Float32, 256) dan
  `samp_topk_idx` (Int32, 256), dialokasikan di blok setup decode.
- Env baru: `BONSAI_TOP_K` (default 20), `BONSAI_TOP_P_X1000` (950),
  `BONSAI_MIN_P_X1000` (0). **`BONSAI_LOGIT_FLOOR_X10` DIHAPUS.**
- `BONSAI_REP_PENALTY_X100` tetap ada tapi default **100 = MATI** (tidak ada di
  spesifikasi resmi -> tidak boleh diasumsikan).

**Plumbing:** `infer_susah/deploy_infer.sh` (blok PYSAMP + echo), ringkasan
config di `tes_infer_susah.sh`, dan `infer_susah/infer_config.json` (preset
thinking: temp 0,70 / top-k 20 / top-p 0,95 / min-p 0 / rep 100=mati).

**Verifikasi:**
- `bash -n` kedua skrip lolos; env yang diekspor sudah benar (diperiksa langsung).
- Gate type-check lokal: build hanya berakhir error `Metal Compiler` (codegen
  Apple, SETELAH analisis semantik). Dibuktikan dengan menyuntikkan
  `var _uji_tipe: Int = Float32(1.0)` di dalam fungsi baru -> error tipe
  dilaporkan di baris 239, Metal TIDAK muncul. Jadi kode baru ter-type-check bersih.
- Logika algoritma diuji lewat port Python 1:1: top-k selalu 20 kandidat,
  top-p 0,95 -> 14 lolos, top-p 0,85 -> 5 lolos, min-p 0,1 -> 3 lolos,
  k=1 -> top-1, p=1.0 -> semua lolos, dan tidak ada pilihan di luar top-k.

**BELUM diuji di Kaggle.** Perlu `./tes_infer_susah.sh` dengan sinkronisasi
sumber (main.mojo berubah) — jadi JANGAN pakai `--no-sync`.

---

## Audit jalur BASELINE tanpa KHQ (read-only)

Dipicu pertanyaan pengguna: "ngapain lihat khq, seharusnya tanpa khq ngg hancur".
KHQ dibebaskan lewat eliminasi: `BONSAI_KHQ_PATH` tidak pernah di-export di
`infer_susah/deploy_infer.sh` (grep = 0), jadi output rusak memang berasal dari
jalur baseline fp16, bukan dari kompresi KV.

**Audit inti decode — semuanya BERSIH (tidak ada kerusakan matematis):**
- `src/kernels/elementwise_sm75.mojo` → `partial_rope_sm75_gpu`: sudah HALF-SPLIT
  (index i ↔ i + rotary_dim/2). Fix dari `LAPORAN_STATUS_AUDIT_DAN_PERBAIKAN.md`
  open-issue #2 sudah diterapkan.
- `gqa_attention_sm75_gpu`: `block_dim=(head_dim)` → 256 thread = 8 warp, cocok
  dengan reduksi smem 320 float; interleave q/gate `hq*(2*head_dim)+head_dim+tid`
  benar; offset KV `pos*kv_dim` benar.
- Dimensi GDN dari config: 48 v-head / 16 k-head / 128 / 128 → conv_dim 10240,
  konsisten (`main.mojo:447` + `config.mojo:56-61`).
- Pola layer: `is_linear = (li % 4) != 3` → full attention di li = 3, 7, 11… BENAR.

**Inkonsistensi laten (bukan penyebab sekarang, tapi salah):**
`src/models/qwen3_5/rope.mojo` → `apply_partial_rope` MASIH memakai pasangan
bersebelahan `(2i, 2i+1)` = interleaved, bertentangan dengan kernel GPU dan
spesifikasi half-split. Jalur produksi GPU tidak memanggilnya (hanya jalur CPU
yang sudah dihapus), TAPI `tests/test_qwen3_5_architecture.mojo` memanggilnya →
tes itu memvalidasi formula yang salah.

**Tersangka utama: state GDN fp16 yang rekuren.**
`gated_delta.mojo`: host `state_s` Float32, device `state_s_dev` **Float16**
(`DeviceBuffer[DType.float16]`, komentar: "math di dalam kernel tetap FP32").
Artinya nilai state di-round ke fp16 SETIAP langkah, selama 48 lapisan linear.
Bandingkan: KV cache (16 lapisan full attn) append-only → error ditulis sekali.
State GDN dikalikan ulang tiap langkah → error menumpuk seiring posisi.
Cocok dengan bukti: sampling T=0.70 sudah diuji dan hanya menghapus loop kaku
(tidak memulihkan koherensi); output pendek ("Hello") benar; keruntuhan berskala
panjang (run cerita tanpa penalaran pun runtuh di token 75).

**Deviasi lain dari perilaku resmi (terverifikasi):**
- Tidak ada stop EOS: `for step in range(max_tokens-1)` tidak pernah break di
  `<|im_end|>` 248046.
- Token PERTAMA selalu argmax (`main.mojo:1052` dan `:1106`) walau sampling aktif.
- `BONSAI_TEMP_X100` default 0 → greedy; `enable_thinking` tidak pernah di-set.

**Belum diuji:** hipotesis GDN fp16 — butuh A/B state fp32 vs fp16 (prompt & seed
sama). BELUM ada perubahan kode apa pun; menunggu perintah pengguna.

---

## RISET INTERNET: config resmi + implementasi referensi (hipotesis TERKONFIRMASI)

Sumber: `prism-ml/Bonsai-27B-mlx-1bit/config.json` (raw HF), `mlx-lm`
`qwen3_5.py` + `gated_delta.py` + `qwen3_next.py`, docs.prismml.com, dan
ollama/ollama#15865. **Catatan pengguna: jangan cari di lokal — riset harus dari internet.**

**TEMUAN KUNCI — config resmi menyatakan dtype state secara eksplisit:**
```json
"mamba_ssm_dtype": "float32"
```
Jadi hipotesis "state GDN harus fp32" BUKAN asumsi lagi — itu deklarasi resmi model.
Kita menyimpannya **Float16** (`gated_delta.mojo`, `DeviceBuffer[DType.float16]`).

**Referensi mlx-lm `gated_delta.py`:** `state = mx.zeros((B,Hv,Dv,Dk), dtype=mx.float32)`;
gate kernel packed mensyaratkan `state.dtype == mx.float32`; kernel cast balik dengan
`static_cast<StT>` (dtype state), bukan dtype input.

**Bug publik yang identik:** ollama/ollama#15865 — kernel GDN menulis state balik di
dtype input (bf16) alih-alih fp32. Gejala: "emits one or two semi-correct tokens, then
collapses into degenerate repetition"; mekanisme: "each recurrent step round-trips state
through bf16, losing ~0.4% relative precision per step. Decay terms close to 1.0 compound
this drift across 100s of decode steps until state is meaningless." Fix = state fp32.
(Persis pola kita: koheren ~50-100 token lalu runtuh.)

**Paritas config resmi vs implementasi kita (semua COCOK):**
model_type `qwen3_5`; 64 layer; `layer_types` = 3 linear + 1 full (pola li%4!=3 identik);
hidden 5120; intermediate 17408; head_dim 256; H_q 24 / H_kv 4; partial_rotary_factor 0.25
(rotary_dim 64); rope_theta 10000000; `rope_type: "default"`; linear 48 v-head / 16 k-head /
128 / 128; conv kernel 4; `attn_output_gate: true`; rms_norm_eps 1e-6;
`quantization: {group_size: 128, bits: 1}`.

**Dua item terbuka:**
1. `src/models/qwen3_5/rope.mojo` (CPU) masih INTERLEAVED; config bilang
   `rope_type: "default"` = half-split. Kernel GPU kita sudah half-split (benar).
2. `output_gate_type: "swish"` — belum diverifikasi: bisa merujuk gate output attention
   (kita `sigmoid`) atau gate z GDN (kita `silu`). JANGAN diasumsikan.

**Bukan bug:** KV cache fp16. Model card menyatakan peak diukur dengan FP16 cache
sebagai kasus konservatif; 4-bit KV adalah opsi kompresi, bukan syarat.

---

## AUDIT PARITAS vs llama.cpp PrismML (unduhan ke references/)

**Unduhan:** `references/llama.cpp-prism` — fork `PrismML-Eng/llama.cpp`, branch
`prism`, commit `d8f26ee` (8 Sep 2026). Ini runtime acuan resmi Bonsai (binary
pre-built PrismML berasal dari sini).

**BUG TERKONFIRMASI — dtype state rekuren (3 sumber independen sepakat):**
1. config resmi: `"mamba_ssm_dtype": "float32"`
2. mlx-lm `gated_delta.py`: `state = mx.zeros(..., dtype=mx.float32)`
3. llama.cpp PrismML `src/llama-model.cpp:2774-2791`: `recurrent_type_k/v = GGML_TYPE_F32`
   (berlaku untuk QWEN35/QWEN3NEXT/QWEN35MOE); jalur recurrent murni di :2720-2723 juga F32.

Kita: `gated_delta.mojo` → `state_s_dev = DeviceBuffer[DType.float16]`, dan kernel
`gdn_recurrence_sm75_gpu` (elementwise_sm75.mojo:447-473) menulis balik fp16
**dua kali per langkah**. Persis pola `ollama/ollama#15865` (di sana bf16).
KV cache attention tetap F16 — itu BENAR (referensi pakai `params.type_k/type_v`).

**Yang COCOK (diverifikasi):** pola layer `(i+1)%4!=0`; dense FFN; post-attention norm;
gate attention = sigmoid; gate z GDN = silu; decay `exp(-exp(A_log)·softplus(a+dt))`;
beta = sigmoid; conv+silu; RMSNorm keluar GDN berukuran head_v_dim; Q/K norm RMS dengan
skala inv_scale²/inv_scale; rotary_dim 64; rope_theta 1e7; dimensi GDN 48/16/128/128.

**Pertanyaan lama yang TERSELESAIKAN:**
- `output_gate_type: "swish"` = gate **z GDN** (`ggml_swiglu_split`), bukan gate
  attention. Gate attention = sigmoid. Kita sudah benar di keduanya.
- RoPE: QWEN35 pakai `LLAMA_ROPE_TYPE_IMROPE`; `ggml.h:1877` bilang
  "interleaved M-RoPE, **still NEOX ordering**" → half-split. Kernel GPU kita BENAR.
- Q/K norm: llama.cpp pakai `ggml_l2_norm` view gabungan; mlx-lm pakai rms_norm +
  skala dilipat. Ekuivalen. Kernel kita memang RMS: `1/sqrt(sum_sq/head_dim+eps)*scale`.

**Item referensi yang kita abaikan (aman):** blok MTP/NextN
(`mtp_num_hidden_layers: 1`) — llama.cpp memuat tapi tidak menjalankannya di pass
utama; DSpark drafter (speculative decoding, opsional).

**Hasil tes sampler resmi (background ETcQeV, selesai):** sampling resmi
(temp 0.7 / top-k 20 / top-p 0.95) TETAP degenerasi pada 512 token — RUN 2 (cerita)
dan RUN 3 (soal kolam) loop. Konsisten dengan hipotesis state: sampler bekerja di
atas logits, tidak bisa memulihkan state rekuren yang sudah terkontaminasi.

**Laporan lengkap:** `LAPORAN_AUDIT_PARITAS_LLAMACPP_PRISM.md` (root proyek).

---

## FIX DITERAPKAN — state GDN fp16 → fp32 (2026-09-12, sesi lanjutan)

Perubahan kode (4 file, tidak menyentuh jalur lain):
1. `src/models/qwen3_5/gated_delta.mojo` — `state_s_dev` + `state_s_dev_buf`
   `Float16`/`DType.float16` → `Float32`/`DType.float32`; `init_device` tidak lagi
   mengisi nol lewat buffer FP16 sementara, tapi `enqueue_copy` langsung dari
   `self.state_s` (host, FP32, sudah nol setelah `reset()`).
2. `src/kernels/elementwise_sm75.mojo` — `gdn_recurrence_sm75_gpu`: parameter
   `state_s` dipisah dari template `T` → tetap `UnsafePointer[Float32]`; hapus
   `.cast[T]()`/`Scalar[T](...)` pada store state (4 lokasi: vektor decay, skalar
   decay, vektor update, skalar update). Aktivasi lain tetap fp16 via `T`.
3. `src/csrc/qmv_sm75_kernel.cu` — `gdn_seq_sm75_kernel`: `__half* state` →
   `float* state`; hapus `st[i] = __half2float(__float2half(sdec))` (baris 606) dan
   `st[i] = __half2float(__float2half(snew))` (baris 619); writeback akhir jadi
   `sp[sidx] = st[i]` (float). Launcher `launch_gdn_seq_sm75_fp16`: `__half* sp`
   → `float* sp` (ABI tetap `void*`, jadi tidak ada perubahan tanda tangan).
4. `src/ops.mojo` — alias `CudaGdnSeqFnFP16` param state → `UnsafePointer[Float32]`;
   `gdn_seq_sm75_try_launch` param `state` → `Float32`;
   `gdn_recurrence_sm75_launch_on` param `state_s` → `Float32` (lepas dari `T`).

**Verifikasi lokal:** `mojo package src` → **0 error**, menghasilkan
`bonsai_check.mojopkg` 4.99 MB. `mojo build main.mojo` lolos analisis semantik +
instansiasi generik penuh (`gdn_recurrence_sm75_launch_on[float16]`), hanya gagal di
tahap codegen Apple Metal (`Metal Compiler failed to compile metallib`) — gate lokal
yang sudah diketahui, tidak relevan untuk target CUDA sm_75.

**PENTING — jebakan lingkungan (Mounty/NTFS):** drive eksternal ini menghasilkan file
AppleDouble `._*` di dalam `src/` (mis. `.___init__.mojo`, `._common.mojo`,
`._kernels`). Loader paket Mojo membacanya sebagai modul → error palsu
"unable to locate module '_decode_sm75'" dan "attempted relative import with no known
parent package". Solusi: `rsync -a --exclude '._*' src/ /tmp/bonsai_build/src/` lalu
build dari `/tmp`. Jangan simpulkan repo rusak dari error itu.
Catatan tambahan: `pixi run` juga rusak karena `conda-meta/._*.json`; pakai binary
langsung + `MODULAR_HOME="<prefix>/share/max"` (bukan `<prefix>`).
`src/khq/calib.mojo` punya `fn main` yang membuat `mojo package` gagal
(pre-existing; deploy script mengandalkan `|| true`) — di salinan /tmp di-rename
agar type-check tuntas.

**Belum dijalankan:** A/B test prompt+seed identik di T4 (butuh rebuild
`scripts/build_cuda_ffi.sh` untuk `.cu` — nvcc tidak ada di macOS), dan
`src/models/qwen3_5/rope.mojo` masih pakai pasangan interleaved `(2i,2i+1)` —
kontradiksi laten dengan kernel GPU half-split (hanya dipakai test).

---

## GERBANG REGRESI KOHERENSI DITAMBAHKAN + PUSH KAGGLE (2026-09-12, sesi lanjut 2)

**Temuan penting:** test T4 yang ada (Run 1-3 di `deploy_on_kaggle.sh`) hanya
menghasilkan **24 dan 8 token**. Degenerasi baru muncul di 50-100+ token, jadi
pipeline lama akan "LULUS" walaupun bug masih ada. Karena itu ditambahkan gerbang baru.

**Baseline RUSAK sudah terdokumentasi** di `infer_susah/HASIL_TES_PEMISAH.md`
(T=0.70 seed 1234, 512 token): cerita = rasio id unik 0.28 (siklus @ token 75);
penalaran = 0.21 (siklus @ 207). Sebaran per-50 jendela ikut tercatat di sana.

**Yang ditambahkan** — `deploy_on_kaggle.sh`, blok "Run 4" (setelah Run 3, sebelum 5b KHQ):
- 2 prompt panjang (cerita + soal kolam, sama seperti tes pemisah) × **512 token GREEDY**
  (`BONSAI_TEMP_X100=0` → deterministik, reproducible).
- Gerbang 2 syarat, keduanya objektif tanpa ambang statistik teks:
  (a) 96 token terakhir TIDAK periodik persis (p ≤ 48);
  (b) 50 token TERAKHIR punya ≥ 12 token unik.
- Default NON-FATAL (agar KHQ tetap jalan); `BONSAI_COH_FATAL=1` → exit 1.
- Mencetak rasio unik total, rasio ekor, siklus@, sebaran per-50 jendela, dan
  baseline rusak sebagai pembanding langsung.

**Jebakan yang ketemu saat menulis gerbang:** versi pertama memakai jendela
sejajar-depan (`g[450:500]`), sehingga ekor yang justru paling rusak tidak dinilai →
kasus degenerasi realistis lolos. Sudah diperbaiki: gerbang memakai `g[-50:]`.
Diuji dengan 3 log sintetis: sehat → LULUS; loop periodik p=3 → GAGAL;
degenerasi non-periodik (4 nilai unik di ekor) → GAGAL. Semua sesuai harapan.

**Dijalankan:** `bash push_to_kaggle.sh` (background task rFRNwy) — sinkron dataset
`okiabrian/bonsai-mojo-source` lalu push kernel `okiabrian/bonsai-mojo-t4-build`.
Kernel menjalankan `deploy_on_kaggle.sh` di T4: kompilasi ulang `.cu` (nvcc, jadi fix
fp32 ikut), build `main.mojo`, test suite, lalu gerbang baru.

**Lokasi hasil:** `dist_kaggle_mojo/` (`build_log.json`, `infer_t4.log`,
`coherence/*.log`), log mentah `/tmp/push_kaggle.log`.

---

## HASIL RUN KAGGLE v119 — FIX fp32 TIDAK MENYEMBUHKAN DEGENERASI (2026-09-12)

Kernel `okiabrian/bonsai-mojo-t4-build` **versi 119** selesai, exit code 0.

**Fix terbukti AKTIF** (bukan salah jalur): nvcc 12.8 membangun ulang `.so` 2.0 MB;
simbol ter-link `gdn_seq_sm75_kernelILb1ELi2EEvPK6__halfS2_S2_S2_S2_PKfS4_PfPS0_...`
→ argumen state = `float*` (bukan `__half*`); 49 register, 0 spill.
Jalur decode juga benar: `forward_gpu`(layer.mojo:337) → `qwen3_5_gdn_step_gpu` →
`gdn_recurrence_sm75_launch_on` → `gdn_recurrence_sm75_gpu` (state fp32).

**Test lama SEMUA LULUS:** smoke FFI, QMV decode, QMM (max_err ≈ 0.0077), RoPE
(worst_err 0.0), build main.mojo.

**GERBANG KOHERENSI GAGAL** (512 token, GREEDY):
- cerita: 82 unik (0.16), siklus p=2 @ token 166, ekor 50 token cuma 2 unik
- penalaran: 33 unik (0.06), siklus p=7 @ token 48, ekor cuma 7 unik
- sebaran cerita: 0.74 0.70 0.58 0.30 0.04 0.04 ... (runtuh ±token 200)
- sebaran penalaran: 0.66 0.14 0.14 ... (runtuh ±token 50)

**Isi teks (detok `tokenizers` dari tokenizer.json lokal):** kedua run masuk mode
thinking (`Here's thinking process:`) lalu loop — cerita loop `* * *`, penalaran
loop `**Context:**`. **Tidak pernah menyentuh jawaban.**

**Tiga pembatas kesimpulan (JANGAN divonis berlebihan):**
1. Baseline lama = sampling T=0.70; gerbang baru = GREEDY → belum apple-to-apple,
   A/B dtype state yang sesungguhnya BELUM dilakukan.
2. `apply_chat_template(add_generation_prompt=True)` tanpa `enable_thinking=False`
   → template menyisipkan `<think>` (temuan lama #1 di HASIL_TES_PEMISAH.md).
3. **Tidak ada test diferensial untuk kernel GDN** — QMV/QMM/RoPE/Argmax ada,
   rekurensi delta-rule tidak. Kernel yang baru diubah justru tanpa jaring pengaman.

**Kesimpulan:** fix fp32 DIPERTAHANKAN (memang yang dilakukan config/mlx-lm/llama.cpp),
tapi ia BUKAN akar degenerasi (atau bukan satu-satunya). Bukti condong ke
**loop decoding di dalam mode thinking**, bukan akumulasi galat numerik.

**Laporan:** `LAPORAN_HASIL_GERBANG_KOHERENSI.md`. Teks hasil + log kernel v119 di
`dist_kaggle_mojo/koherensi/`. Log mentah build di `fix/dist_kaggle_mojo/build_log.json`.

**Langkah berikut (urut nilai):** (1) `enable_thinking=False`, greedy 512 token —
tes termurah & paling menentukan; (2) sakelar `BONSAI_GDN_STATE_FP16=1` untuk A/B
dtype state sungguhan; (3) test diferensial kernel GDN vs FP64; (4) stop di EOS.

---

## Sesi lanjutan — TEST PRESISI STATE GDN (+ temuan bug harness)

**Deliverable "tambah test" SELESAI.** `tests/test_gdn_state_precision.mojo` ditulis
ulang dan **SEMUA PASS**: 1 langkah 1,66e-07; 64 langkah 1,60e-06; **512 langkah 1,53e-06**;
64 noparams 1,85e-07; 64 rep2 D=16 1,65e-06. Toleransi 1e-4 (di tengah antara FP32
~1,5e-06 dan FP16 ~1,3e-03 → daya-beda ~1000x). Test mencetak pembanding FP16 tiap kasus.

**TEMUAN BARU & PENTING — bug harness:** `ctx.enqueue_function` **mengabaikan offset
pada argumen pointer device**. `buf.unsafe_ptr()` benar; `buf.unsafe_ptr().offset(n)`
(n>0) → kernel membaca SAMPAH. Terbukti dengan kernel probe minimal, terjadi bahkan
pada launch PERTAMA di konteks segar; `offset(1)` juga gagal; menyimpan pointer ke `var`
tidak menolong; **buffer terpisah (offset 0) selalu benar**.

Rantai penyempitan: (a) state persisten & kernel EKSAK — `max|S_b - ref(S_a)| = 1,4e-09`
pada dua launch identik; (b) 4 varian isolasi → pemicunya "input berubah antar-launch";
(c) kernel probe → pointer offset tidak valid.

**KOREKSI kesimpulan v119:** kegagalan test multi-langkah versi LAMA adalah **artefak
harness**, bukan sinyal numerik. Jadi bukti "fp32 bukan akar masalah" yang berasal dari
test itu **tidak sah**. Yang masih sah: gerbang koherensi gagal (macet di mode thinking).
Fix fp32 sekarang **terbukti benar DAN terukur**.

**PERLU DIVERIFIKASI DI CUDA (prioritas tertinggi):** `layer.mojo` baris **484-493**
(`causal_conv1d_sm75_launch_on` per-token, TANPA syarat saat prefill) dan baris **523-537**
(fallback GDN) memakai pola `.offset()` ini. Kalau bug ini juga terjadi di CUDA →
prefill M>1 pakai data sampah untuk t>=1 → kandidat kuat akar degenerasi.
**TAPI** pengamatan ini di backend **Apple Metal lokal**; mungkin spesifik Metal.
`tests/test_qmm_gpu.mojo:195` juga pakai `.offset()` tapi lewat **CUDA FFI** (pemanggilan
fungsi C langsung) — mekanisme berbeda, bukan bukti. → verdict harus dari T4.

**File:** `tests/test_gdn_state_precision.mojo` (ditulis ulang), `tests/test_enqueue_offset.mojo`
(baru; non-fatal, `BONSAI_OFFSET_STRICT=1` untuk fatal), `deploy_on_kaggle.sh` (kedua test
masuk gerbang korektness; `BONSAI_GDN_STATE_FATAL=1` untuk fatal). Laporan:
`LAPORAN_TES_PRESISI_STATE_GDN.md`. Kedua test terpaket otomatis (`cp -r tests`).

### Koreksi arah: FOKUS T4, hentikan bahasan backend lokal

Pengguna menegaskan: **"fokusnya t4, ngg usah lihat metal"**. Konsekuensinya:

* Semua hasil GPU lokal (termasuk verdict offset) **bukan bukti untuk T4** dan tidak
  boleh dijadikan dasar kesimpulan. Angka test GDN lokal juga hanya indikatif.
* Semua referensi "Metal" sudah dihapus dari `tests/test_enqueue_offset.mojo`,
  `deploy_on_kaggle.sh`, dan `LAPORAN_TES_PRESISI_STATE_GDN.md`. Verdict test offset
  dibuat netral + dicatat bahwa hasil otoritatif = run di T4.
* **Jangan ulangi spekulasi backend.** Kalau butuh jawaban GPU, jalankan di T4.

**Konfirmasi jalur eksekusi (penting):** kernel `okiabrian/bonsai-mojo-t4-build` adalah
`kernel_type: script`, `code_file: run_deploy.py`, `machine_shape: NvidiaTeslaT4`,
`enable_gpu: true`. `run_deploy.py` mengekstrak `mojo_source.tar.gz` lalu menjalankan
`bash deploy_on_kaggle.sh` — jadi blok test di deploy_on_kaggle.sh PASTI ikut jalan di T4.
Ada cache build (`mojo_build_cache.tar.gz`) sehingga `mojo run` berikutnya lebih cepat.

**Run diluncurkan** (`push_to_kaggle.sh`, log lokal `/tmp/kaggle_push_t4.log`). Yang dibaca
dari log T4: (1) verdict `test_enqueue_offset.mojo` — apakah offset pointer device
dihormati; (2) PASS/GAGAL `test_gdn_state_precision.mojo`. Artefak diunduh ke
`fix/dist_kaggle_mojo/` (`.whl` + `build_log.json`).

### HASIL T4 v120 (SELESAI, 40m24s) — dua hipotesis numerik GUGUR

**1. Offset pointer device: BERFUNGSI di T4.** Ketiga kontrol BENAR:
`pointer dasar 0.10998535`, `pointer offset(4) 0.9902344`, `buffer terpisah 0.9902344`
→ `VERDICT: offset pointer device BERFUNGSI`.
→ **Hipotesis "prefill rusak karena offset" MATI.** layer.mojo 484-493 & 523-537 AMAN.

**2. Presisi state GDN fp32: SEMUA PASS di T4.** step1 5,50e-08 | step64 1,67e-06 |
**step512 1,58e-06** | noparams 6,15e-08 | rep2 D=16 1,60e-06. Toleransi 1e-4.
Daya-beda vs FP16 839-5512x. Angka hampir identik dengan mesin pengembang
(1,53e-06 vs 1,58e-06 @512) → fix fp32 **terbukti benar di hardware target**.

**3. Gerbang koherensi TETAP GAGAL, tanda tangan IDENTIK v119:** cerita 82 unik /
rasio 0.16 / siklus p=2 @166; penalaran 33 unik / 0.06 / siklus p=7 @48. Sebaran cerita
0.74 0.70 0.58 0.30 0.04... → degenerasi **bukan** dari akumulasi galat state, **bukan**
dari prefill. Decode T4 ~15,7 tok/s. EOS tidak pernah muncul. FFI CUDA sehat
(`bonsai_cuda_sm75_init() == 0`, smoke QMV PASS). QMM/RoPE/Argmax juga lulus.

**PELAJARAN MAHAL (catat untuk selamanya):** kegagalan `.offset()` di mesin pengembang
TIDAK tereproduksi di T4. **Hasil GPU backend non-target TIDAK BOLEH dijadikan dasar
kesimpulan tentang produksi**, walau terlihat seperti bug bersih & reprodusibel.
Alur yang benar: rumuskan sebagai pertanyaan → buat test yang mencetak verdict →
jalankan di T4. Pengguna menegur ini ("kok metal? fokusnya t4") dan teguran itu BENAR.
Efek samping: kegagalan test multi-langkah versi lama juga artefak backend lokal.

**Langkah berikut (sudah dipersempit ke perilaku decoding):** (1) `enable_thinking=False`
+ greedy 512 token — paling menentukan; (2) stop di EOS `<|im_end|>`=248046 di main.mojo
(tanpa ini gerbang mengukur 512 token paksa); (3) banding `causal_conv1d` per-token vs
FFI `gdn_seq` (kini bersih dari isu offset). Sakelar A/B dtype state turun prioritas.

**Laporan diperbarui:** `LAPORAN_TES_PRESISI_STATE_GDN.md` (kini berisi verdict T4 v120).
