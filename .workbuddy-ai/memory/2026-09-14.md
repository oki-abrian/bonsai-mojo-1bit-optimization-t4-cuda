# 2026-09-14 — Bonsai-27B 1-bit (T4 / sm_75)

## Run inferensi TUNGGAL via jalur ramping `infer_susah` (tanpa KHQ)

**Permintaan user:** jalankan 1x inferensi saja, limit `max_tokens` 2048, ingin lihat
apakah hasilnya sudah bagus. Jalur wajib ramping — **bukan** `deploy_on_kaggle.sh`
(yang menjalankan rangkaian KHQ: dump → kalibrasi → runtime).

**Yang dijalankan:** `./tes_infer_susah.sh` (kernel `okiabrian/bonsai-infer-susah` versi 9).
Jalur ini hanya: sync sumber ke dataset `okiabrian/bonsai-mojo-source` → `nvcc` sm_75 →
`pixi run mojo build main.mojo` → 1 prompt. Tidak ada self-test / KHQ / A/B.

**Config:** diubah dari mode 3-prompt (96/512/512) menjadi **prompt tunggal**,
`max_tokens = 2048`, prompt = soal penalaran pipa A/B. Sampling = resep resmi Bonsai
(temp 0.70 | top-k 20 | top-p 0.950 | min-p 0 | rep_penalty MATI | seed 1234).

**Hasil: BERHASIL dan BENAR.**
- Jawaban akhir **3 jam** — benar (5/12 → 5/6 → sisa 1/6 → 1 jam → total 3 jam).
- Mode thinking aktif: mengeluarkan `</think>` lalu jawaban akhir berbahasa Indonesia.
- `>> [STOP] token henti 248046 pada langkah 1738 — generasi dihentikan`
  → **FIX-2 (stop-token) terbukti bekerja di produksi** (dulu loop mengabaikan EOS).
- 1739 token ter-generate, 0 error, exit code 0.
- Perf: prefill 81 token = 641 ms (126,4 tok/s) · decode 60,66 ms/token (16,48 tok/s) ·
  total 106,2 s.
- **Satu-satunya cacat:** kebocoran bahasa Tionghoa `第一阶段` / `第二阶段` di kalimat
  Indonesia (seharusnya "tahap pertama"/"tahap kedua"). Diduga artefak kuantisasi
  1-bit (1,125 bpw), bukan bug alur hitung — jawabannya tetap benar.

**Laporan lengkap:** `HASIL_INFER_SUSAH_2026-09-14.md` (repo root).
Log mentah: `/Users/macmini/.mounty/SSD_External/fix/dist_kaggle_mojo/infer_susah/infer_susah.log`.

**Perbaikan tambahan hari ini:** catatan basi di `detok_infer.py` ("main.mojo TIDAK
berhenti di EOS") dikoreksi — sudah tidak benar sejak FIX-2; sekarang menyatakan
main.mojo berhenti di token henti.

## Catatan lingkungan
- Toolchain Mojo lokal di `.pixi/envs/default` **tidak bisa kompilasi** lokal
  (`unable to locate module 'stdlib'`, `MODULAR_HOME` diabaikan). Repo ada di mount
  macFUSE. Karena itu verifikasi kompilasi **hanya** lewat Kaggle T4 — dan build di
  sana sukses (tidak ada error kompilasi pada sumber hasil edit).
