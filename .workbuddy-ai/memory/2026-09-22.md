# 2026-09-22

## 06:24 — Produksi kini mendukung 1-bit DAN 2-bit sekaligus (BONSAI_BITS)

User menegaskan keras: "jangan beri pilihan gitu, 1 bit dan 2 bit itu
kepakai bukan salah satu dipilih yang mana, ini salah. jadi keduanya
penting". Kesalahan saya sebelumnya: menyajikan "pemilih vs bawaan" sbg
pilihan A/B yg mesti dijawab user. Keduanya WAJIB berjalan.

Diedit (izin diberikan via "iya"):

1. `kernel-metadata.json` — `dataset_sources` ditambah
   `okiabrian/bonsai-2bit-weights`. Kedua dataset termount bersamaan.
2. `deploy_on_kaggle.sh` §5 (baris ~724-750) — pemilihan model:
   ```
   BONSAI_BITS="${BONSAI_BITS:-1}"
   export BONSAI_BITS
   if [ "$BONSAI_BITS" = "2" ]; then
       MODEL_CANDS=".../bonsai-2bit-weights ..."
   else
       MODEL_CANDS=".../bonsai-27b-mlx-1bit ..."   <- identik dgn kode lama
   fi
   KMODEL=""
   for cand in $MODEL_CANDS; do ... done
   ```
   Bawaan 1 => kandidatnya SAMA PERSIS dgn kode lama => perilaku 1-bit
   tidak berubah sedikit pun.
3. `deploy_on_kaggle.sh` blok export (~831) — `export BONSAI_DECODE_H2=1`.
   Aman global: linear.mojo:409 mensyaratkan `self.bits == 2`, jadi pada
   1-bit diabaikan. Tanpa ini 2-bit diam-diam skalar (75,25 vs 58,48).
4. Cache KHQ DIKUNCI PER MODEL (`KV_CACHE_BIN/LOG`, `KV_OUT_BIN/LOG`
   memuat `b${BONSAI_BITS}`). Nama kerja `$KHQ_DIR/kv_dump.bin` &
   `$DIST_DIR/khq_dump.log` SENGAJA tetap, supaya pembaca 5b.2 & 5b.9
   tidak tersentuh. Tambahan: salinan bernama per-model juga ditulis ke
   KHQ_DIR & DIST_DIR agar run berikutnya bisa mengimpornya dari output
   kernel (yang terbit memang dua direktori itu).

Verifikasi: `bash -n` OK | JSON OK | nol sisa referensi
`$CACHE_DIR/kv_dump.bin` / `$CACHE_DIR/khq_dump.log` | shebang bash, hanya
`set -e` (TANPA `set -f`, jadi glob `$MODEL_CANDS` mengembang) | uji fungsi
nyata: input "" -> 1-bit, "1" -> 1-bit, "2" -> 2-bit.

TEMUAN PENTING: paket 2-bit adalah MODEL BERBEDA (Bonsai-2,
`prism_hadamard_qwen35`, schema_version 2, butuh hadamard.json), BUKAN
Bonsai-27B dalam lebar bit lain. Keduanya sear-sitektur (27B hibrida
Qwen3.5 3:1, 64 layer, hidden 5120, intermed 17408, vocab 248320).
Bobot 2-bit SUDAH ADA sbg dataset Kaggle (8.595.477.990 byte) — komentar
lama di run_infer_2bit.py yg bilang belum ada sudah BASI.

KOREKSI DIRI: saya pernah menduga gerbang produksi bergantung pada patokan
yg direkam utk 1-bit. SALAH — gerbang koherensi itu deteksi loop +
keragaman ekor + gerbang-C swakalibrasi (deploy_on_kaggle.sh:1227 menyebut
nilai BASE hanya KONTEKS, bukan ambang). Uji tokenizer 5.0 memakai
config.json checkpoint itu sendiri. Jadi gerbang-gerbangnya model-agnostik.
Yang benar-benar bermasalah hanya cache KHQ (terikat "bobot tetap", :1266).

BELUM DIJALANKAN & BELUM DI-COMMIT. Perlu run produksi dgn BONSAI_BITS=2
(plus KHQ_DUMP_FORCE=1 sekali krn cache lama basi) utk membuktikan
2-bit jalan di pipeline produksi.

=========================================================
RUN PRODUKSI 2-bit PERTAMA (kernel v131, 14 mnt 19 s) — GAGAL DI GERBANG 5.0
=========================================================
Yang SUDAH LULUS (bukti dari log bonsai-mojo-t4-build.log):
- §4a sidik jari build-cache COCOK: "[BUILD-CACHE] bonsai_infer dipakai
  ulang dari cache (sidik jari cocok, 949904 byte). Kompilasi DILEWATI".
  (Blokir sebelumnya hilang setelah CPU build v29 dijalankan.)
- FFI smoke test: [SUCCESS] Primary Context sharing Mojo <-> CUDA FFI.
- Pemilih model JALAN: ">> [T4] Bobot ditemukan (tanpa unduh):
  /kaggle/input/bonsai-2bit-weights (BONSAI_BITS=2)".

Yang GAGAL (t=248 s, run dihentikan exit 1):
  >> [TOK-FAIL] tes tokenizer GAGAL — run dihentikan, jangan lanjut.
  - <|im_end|> -> vocab id 248046, diharapkan 248044 (im_end)

AKAR MASALAH (terverifikasi dari berkas asli, bukan dugaan):
- 1-bit Bonsai-27B (model_type qwen3_5): bos=248044 eos=248046.
- 2-bit Bonsai-2 (prism_hadamard_qwen35): bos=248044 **eos=248044**, dan
  added_tokens_decoder: 248044=<|endoftext|>, 248045=<|im_start|>,
  248046=<|im_end|>. tokenizer_config.json 2-bit menyebut
  eos_token="<|im_end|>" — JADI PAKET 2-bitNYA SENDIRI TIDAK KONSISTEN
  (config.json bilang eos=248044=<|endoftext|>, tokenizer_config bilang
  eos=<|im_end|>=248046).
- src/tokenizer.py:_load_special_ids() memaksa im_end_token_id = eos_token_id
  dari config. Itu benar utk Bonsai-27B (keduanya 248046) tapi SALAH utk
  Bonsai-2 (menjadi 248044, padahal <|im_end|> = 248046).
- Gerbang 5.0 lalu menuntut convert_tokens_to_ids("<|im_end|>") == im_end
  => 248046 != 248044 => GAGAL.
- BUKTI PERILAKU: model 2-bit benar-benar mengakhiri giliran dgn 248046
  (run uji v44 berhenti di 781/766 token, max 2048; main.mojo:1836
  mematok stop default 248046). Jadi yg benar utk dihentikan = 248046.

RENCANA PERBAIKAN (belum dikerjakan, butuh izin utk deploy_on_kaggle.sh):
1. src/tokenizer.py: setelah tokenizer dimuat, resolusi im_start/im_end dari
   vocab tokenizer sendiri (fallback ke nilai kini). Efek 1-bit = NOL.
2. deploy_on_kaggle.sh:780-781: longgar-kan "im_end harus == eos config"
   jadi "im_end harus == id <|im_end|> dari vocab".

=========================================================
RUN PRODUKSI 2-bit TANPA KHQ (kernel v133, 16 menit, exit 0) — SEMUA LULUS
=========================================================
Sakelar `BONSAI_KHQ_ENABLE` dipasang (atas perintah user "dimatikan dulu gpp,
kan ini dicoba dulu tanpa khq"). Sekali ini saya awalnya TIDAK mematikannya
dan menjalankan dgn KHQ aktif (39 menit) — kesalahan saya yang ditegur user.

Hasil run `BONSAI_BITS=2 BONSAI_KHQ_ENABLE=0`:
- `[KHQ] 5b DILEWATI (BONSAI_KHQ_ENABLE=0)` tercetak; cache KHQ tak disentuh.
- Jalur h2 aktif (1156 baris `[H2] jalur decode cepat (PRMT+HFMA2) aktif`).
- Gerbang koherensi GREEDY 512 token LULUS (rc=0):
  - cerita.think0: 307 token (EOS di posisi 306), 48,16 ms/token,
    rasio unik awal50/ekor50 = 42/38, tanpa loop, nol CJK.
  - penalaran.think0: 512 token, 51,31 ms/token, 45/37, tanpa loop, nol CJK.
- Teks penalaran (didecode lokal): 1/6 + 1/4 = 5/12; ×2 jam = 5/6 terisi,
  sisa 1/6, pipa A sendiri = 1/6 per jam → 1 jam ⇒ total 3 jam (benar).
  Terpotong di 512 token tepat saat menuliskan "Misalkan waktu tambahan".
- Konteks pendek: 45,64–49,07 ms/token (prompt 9/98 token).
- Run KHQ aktif sebelumnya juga lulus semua gerbang, exit 0, jawaban sama
  (3 jam, benar), nol CJK.

CATATAN: 48–51 ms/token ini lebih rendah daripada 58,48 ms (uji v44 sesi
sebelumnya), tapi itu lintas-sesi — drift mesin Kaggle 5–7% membuat selisih
<8% tak bisa dinilai. Bukan perbaikan, bukan regresi.
