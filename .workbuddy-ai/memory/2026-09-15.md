# 2026-09-15

## Baca referensi baru (permintaan user: "baca keduanya")

1. `references/KHQ-1-2-pre-opti-2-o-edit2-testall-benerin-v-backup-copy-copy-3.41-multi-input-test-4bit-residual/OjaKV_Adaptive_Token_Selection.md`
   - Rancangan arsitektur KV cache hibrida untuk sisi KHQ (KudaHitamQuant, MLX/training).
   - **Split-Cache 256/64**: K/V per layer dipecah jadi Base [seq,64] (selalu dibaca) + Residual [seq,192] (hanya dibaca untuk token "penting").
   - Pemetaan pas dengan Bonsai-27B: head_dim=256, rotary_dim=64 (partial_rotary_factor 0.25) → 64 dim kena RoPE = Base (tidak dikompres), 192 dim semantik = Residual.
   - Seleksi token penting: lightweight attention 64-dim (`Q_64 @ K_base_64.T`) + top-K/threshold adaptif, bukan NN terpisah.
   - OjaKV: proyeksi ortogonal W [256x64] di-update online via Oja's rule per blok (32 token) + re-ortonormalisasi QR; token tak penting direkonstruksi `x_approx = W·y` on-the-fly.
   - Anchor tokens: token dengan reconstruction error tertinggi disimpan full-rank permanen (attention sinks).
   - Tambahan Neural-Enhanced (§7): NBN micro-autoencoder (192→96→48), IPN importance predictor (5 fitur, ~216 params), CLIP cross-layer importance, ECR residual network — total ~4.2MB/layer, online training saat decode.
   - Evolusi §9: SMEC (Matryoshka gating, offline-trained, identitas-eye init) + UnRoPE → zero overhead online; pipeline: UnRoPE → SMEC filter → split 64/192 → fast-attention seleksi → re-RoPE → FlashAttention.
   - Catatan: ini dokumen rancangan (belum terverifikasi vs paper asli OjaKV/SMEC); angka "60% reduksi bandwidth" adalah klaim desain.

2. `references/mlx-prism-cuda1bit/layerwise.txt`
   - Paper SIGIR 2026 short: "Layer-wise Token Compression for Efficient Document Reranking" + transkrip Q&A web.
   - LTC = adaptive average pooling 1D di layer menengah; layer 1..l*-1 full, layer l*..L terkompresi.
   - Temuan kunci: kompresi layer awal (2-4) merusak kualitas parah; layer menengah (8-14) aman bahkan r=0.4 (nDCG setara baseline, QPS +25% passage / +116% dokumen); kompresi ringan layer akhir bisa malah naik (regularizer).
   - **Compression-aware training wajib** — zero-shot/inference-only compression runtuh pada layer awal (0.287 vs 0.727 nDCG@10 di r=0.2, l=2).
   - LTC ≠ sparse attention (LTC mengurangi fisik jumlah token; sparse membatasi jalur attention) dan ≠ KV eviction (H2O/StreamingLLM membuang dari cache jadi; LTC mencegah cache besar terbentuk).
   - Relevansi ke Bonsai: prinsip "jangan kompres interaksi awal" sejalan dengan split 64-RoPE/192-semantik OjaKV — 64 dim RoPE tidak dikompres.

## Diskusi desain: OjaKV × mixed-precision KV (terinspirasi DeepSeek-V4.1-Flash)

- DeepSeek-V4.1-Flash diverifikasi via web (rilis 10 Sep 2026, MIT): 552B MoE + 196B Engram; CED = 20 encoder kausal + 20 decoder **satu model**; aktivasi asimetris prefill 8B / decode 16B param/token; CSA2 mode Full/Reindex/Reuse (indeks sparse dihitung sekali, reuse lintas layer); FP4 E2M1 + skala E4M3 per 16 channel → 890 B/token KV; HBM ¼. FP4 KV diakui menurunkan stabilitas recall konteks panjang.
- Kesimpulan diskusi dengan user:
  - Bukan 2 model — prefill/decode itu fase, bukan model.
  - Yang bisa dicomot inference-only ke Bonsai: reuse seleksi top-k lintas 16 layer attention (ala CSA2), kuantisasi KV INT4/FP4 per 16 channel (T4 sm_75 tanpa FP4 native — emulate unpack di kernel, pola sama seperti Q1_0 nibble+LUT).
  - Kombo yang disarankan: base 64-d (RoPE) selalu fp16; anchor/top-K → residual fp16; penting → residual FP4 (1.792 B/token/layer); biasa → residual tak dibaca, rekonstruksi W·y (1.024 B ≈ FP4 seragam DeepSeek tapi RoPE tetap full presisi).
  - Angka jujur: di konteks 4096 baca KV hanya ~1,3% step time (60,7 ms/token) → tidak mempercepat; bernilai untuk konteks 16k–64k (hemat ~14% step + VRAM 4 GiB → 2,5 GiB @64k).
  - Pemercepat tok/s di 4096 yang sebenarnya: overhead launch 64 layer (fusi/CUDA graph), bukan KV.
  - User KHQ sudah eksperimen arah ini: folder referensi bernama "...multi-input-test-4bit-residual".

## Edit plan file OjaKV (disetujui user)

- `references/KHQ-.../OjaKV_Adaptive_Token_Selection.md` diubah jadi skema baca bertingkat (keputusan user):
  - tidak penting → residual TIDAK dibaca (0-bit, rekonstruksi W·y, 1.024 B/token/layer)
  - agak penting → residual 1-bit (≈1.216 B)
  - dibutuhkan → residual 4-bit skala per 16 channel (1.792 B)
  - anchor → full-rank fp16 (4.096 B); base 64-d RoPE selalu fp16 di semua tingkat.
- §5 workflow (langkah 4–7) ditulis ulang ke 3 tingkat; ditambah Bab 10 berisi tabel tingkat, justifikasi, catatan implementasi T4 (unpack 4/1-bit ala Q1_0 di qmv_sm75_kernel.cu, amortisasi seleksi lintas 16 layer ala CSA2, parameter OJAKV_TOPK/T_HI/T_LO), dan estimasi dampak (4096: tak terasa; 64k: hemat ~15% step + VRAM 4→~2,5 GiB).

## Koreksi distribusi tingkat (dari user) — FINAL

- Skema final yang dikehendaki user: **anchor = fp16 (jarang, ~1%)**, **4-bit = 10%**, **sisanya (±90%) = 1-bit**. Tidak ada tier "tidak dibaca" sebagai default (0-bit hanya varian opsional yang belum diaktifkan; hematnya hanya ±3%).
- Dokumen Bab 10 sudah disesuaikan penuh: judul, intro, tabel 10.1 (3 tingkat), §10.2 poin 2–3 & 6, §10.3 parameter (`OJAKV_TOPK`, `OJAKV_ANCHOR_Q`), §10.4 tabel estimasi, dan §5 langkah 4–5.
- Konsekuensi penting yang dicatat di dokumen: karena semua token dibaca minimum 1-bit, **rekonstruksi OjaKV (W·y) praktis tidak dipakai** → risiko akurasi terbesar desain OjaKV (kualitas matriks W daring) hilang.
- Rata-rata biaya: 1.302 B/token/layer (3,1× lebih kecil dari fp16 4.096 B). Estimasi: 100k ~15,0 tok/s (fp16 12,4); 200k ~13,7 (9,9); 300k ~12,5 (fp16 18,3 GiB → OOM). Lantai biaya = base 64-d 1.024 B (dimensi RoPE, tak boleh dikuantisasi).

## Koreksi penting (user menahan penyederhanaan saya)

- Saya sempat mengklaim "tidak perlu manajemen W, tidak perlu update per blok, tidak perlu QR" — **user benar, itu penyederhanaan yang salah.**
- Temuan: **dokumen OjaKV mendefinisikan "Base" secara berbeda di dua tempat** — §3.4 Base = proyeksi `x @ W` (perlu W + Oja + QR) vs §4.5/§9.2 Base = slice `x[..., :64]` (dimensi RoPE, tanpa W). Kontradiksi internal yang belum pernah diselesaikan.
- Ditambahkan **§10.5 "Isu Terbuka: Definisi Base dan Peran W"**: tabel perbandingan kedua opsi, **masalah inkonsistensi cache** saat W diperbarui daring (base lama diproyeksikan dengan W lama — merusak untuk rekonstruksi, tidak merusak untuk penyimpanan 1-bit/4-bit), dan eksperimen penentu (bandingkan presisi pemilihan top-10% dari 64 dim RoPE vs 64 dim proyeksi).
- Penunjuk silang ⚠️ ditambahkan di §3.4 dan §4.5 agar kontradiksi ini tidak terlewat lagi.
- Catatan: skema presisi bertingkat (fp16/4-bit/1-bit) **tidak bergantung** pada pilihan ini — hanya menentukan cara Base dihitung & seleksi dilakukan.

## Analisis akibat lengkap (permintaan user: "jangan sederhanakan apapun, berpikir akibatnya")

- Dibuat dokumen baru **`ANALISIS_AKIBAT_KV_BERTINGKAT.md`** — menelusuri akibat tanpa penyederhanaan, tiap klaim diberi status [terukur]/[dihitung]/[belum diuji]. Isi: peta risiko R1–R5; akibat akurasi (SNR 1-bit ±6 dB, error di K menggeser distribusi attention bukan sekadar besaran, bias posisi seleksi dari dim RoPE, seleksi sekali-untuk-16-layer = error konsisten tanpa koreksi, definisi jangkar bila tanpa rekonstruksi); biaya komputasi tersembunyi (dequant 7,4 G elemen/step @300k, biaya QR ±315 GFLOP bila W dipakai ≈1 menit, kuantisasi commit cache); **efek positif yang sebelumnya terlewat** (karena semua token dibaca, tidak perlu gather acak → 3 array linier, bandwidth efisien — kekhawatiran gather saya sebelumnya tidak berlaku); akibat arsitektural (W sebagai state, inkonsistensi cache, chunked prefill wajib, GDN 144 MiB tak tersentuh = 75% layer tanpa manfaat); akibat kode/verifikasi (kernel GQA paling rapuh harus diubah → risiko regresi FIX-1..9; verifikasi hanya via Kaggle ±11 menit/run → rancang batch); batas yang tak bisa dihindari; **eksperimen penentu E1–E4**.
- Koreksi angka saya sendiri: 1.302 B → **1.333 B/token/layer** (versi lama lupa skala & indeks). Tabel §10.4 diperbarui: 100k 2,0 GiB / 6,7 ms / ~14,9 tok/s; 200k 4,0 GiB / 13,3 ms / ~13,6; 300k 6,0 GiB / 20,0 ms / ~12,5. Ditambah peringatan bahwa angka belum termasuk biaya dequant.

## KOREKSI FUNDAMENTAL: arti "1 bit" dan "4 bit" (petunjuk user: "64 dim itu setara 1 bit")

- **SALAH TOTAL sebelumnya**: saya menafsirkan "1-bit / 4-bit" sebagai **format kuantisasi presisi numerik** (sign+skala, INT4/FP4, SNR, byte per grup). Itu **bukan** maksudnya.
- **Arti yang benar**: "1 bit" = **representasi 64 dimensi**, "4 bit" = **representasi 256 dimensi**. Rasio 256/64 = 4 → karena itu disebut 1 vs 4. Yang dibicarakan adalah **berapa dimensi KV yang dibaca per token** (lebar/lebar-baca), bukan berapa bit per nilai.
- Bukti di dokumen (Bab 1–9 asli, sebelum edit saya):
  - §2: token "tidak penting" → hanya Base 64-dim yang dibaca (sisanya di-zero-pad / pakai proyeksi 64-dim). Token "penting" → Base 64 + Residual 192 = 256-dim penuh.
  - §2: "komputasinya turun 75%" — 64 vs 256 dim = 1/4 biaya. Inilah rasio 1:4.
  - §3.3 judul: "Bagaimana OjaKV Menangani Token 'Tidak Penting' (64-dim)?" ← inilah tier "1 bit".
  - §8 klaim "reduksi bandwidth >60%": hitung 10%×256 + 90%×64 = 83,2 vs 256 → **−67,5%**. Konsisten.
- Jadi skema bertingkat user = **skema Bab 2 dokumen itu sendiri, hanya diberi nama**:
  - jangkar (jarang) = full-rank fp16
  - 10% = baca 256-dim penuh ("4 bit")
  - 90% = baca 64-dim Base saja ("1 bit")
- **Konsekuensi**: §10.1 tabel byte saya (216 B / 864 B / 1.792 B / "1-bit = sign+skala per grup") dan seluruh perhitungan SNR/byte di `ANALISIS_AKIBAT_KV_BERTINGKAT.md` §1.0–§2.1 **berbasis arti yang salah** → perlu dibuang/ditulis ulang. Angka "1.333 B/token/layer" juga tidak berlaku (asumsinya kuantisasi).
- Biaya sebenarnya bersifat **dimensi**: base 64-d selalu dibaca; residual 192-d hanya untuk tier atas. Belum ada keputusan format penyimpanan nilai (fp16/8/4) — itu isu terpisah, dan dokumen asli tidak pernah membahasnya.
- Status: **tidak mengedit apa pun** (user: "siapa yang suruh edit?"). Menunggu instruksi.

### Koreksi lanjutan (user: "itu bukan residual+base, asumsi lagi, attention ada 256, tapi pilih 64 doang")

- Kerangka "Base 64 + Residual 192 → digabung jadi 256" **DITOLAK user** — itu asumsi saya. Zero-padding/rekonstruksi juga tidak relevan.
- Versi yang benar (per user): **head attention = 256 dimensi**; tier murah ("1 bit") = attention **hanya memakai 64 dari 256 dimensi**; tier "4 bit" = memakai 256 dimensi. Rasio 256/64 = 4 → asal nama 1 vs 4.
- Jadi yang dipilih adalah **jumlah dimensi yang masuk ke attention**, bukan struktur base/residual.
- Masih TERBUKA (belum dijawab, jangan diasumsikan): (1) 64 dimensi itu dipilih bagaimana — subset tetap atau adaptif per token? (2) apa beda tier "4 bit" (256 dim) dengan jangkar fp16?

## Bab 10 OjaKV diperbaiki (instruksi user: "baca bab 9 dan perbaiki bab 10")

- **Bab 9 dibaca**: SMEC + UnRoPE. §9.1: gating = pengganti NBN/Oja; "menekan dimensi noise ke 0, mendorong dimensi paling informatif ke indeks 1–64"; dilatih offline → zero inference overhead. §9.2: pipeline UnRoPE → SMECAdapter → split `[..., :64]`/`[..., 64:]` → fast-attention → re-RoPE → FlashAttention. **Ini yang menjawab §3.1**: potongan `[..., :64]` valid karena SMEC sudah menyusun ulang dimensi.
- **Bab 10 ditulis ulang** (6 edit):
  - Arti istilah ditetapkan eksplisit: **"1 bit" = 64 dimensi, "4 bit" = 256 dimensi** (rasio 256/64 = 4). Bukan presisi bit.
  - Tabel §10.1: kolom "Base 64-d / Residual 192-d / Biaya baca (byte)" → **"Dimensi yang dipakai attention"** (256 jangkar / 256 "4 bit" / 64 "1 bit"). Semua angka byte + peringatan "format 1-bit belum ditetapkan" **dihapus** (itu asumsi saya yang salah).
  - §10.2 justifikasi ditulis ulang tanpa klaim kuantisasi/SNR.
  - §10.3: catatan T4 soal unpack FP4/Q1_0 dihapus → "1 bit hanya membaca 64 dimensi pertama, 4 bit membaca 256; yang berubah lebar baca, bukan format data". CSA2 & parameter AB-test tetap.
  - §10.4: tabel tok/s + VRAM berbasis byte **dihapus** → diganti dampak dimensional (baca KV turun ±3×; 256 dim hanya dibayar ±11% token).
  - §10.5: "Isu terbuka: definisi Base" (tabel slice vs proyeksi W) **diganti** dengan catatan **Bab 9 menggantikan Bab 3** (SMEC gating, offline, tanpa state W → tanpa inkonsistensi cache).
- **Sisa yang belum dirapikan** (menunggu izin, karena user melarang edit tanpa diminta): penunjuk silang ⚠️ di §3.4 dan §4.5 masih menunjuk ke §10.5 lama; langkah 4–7 di §5 masih memakai istilah "residual 4-bit / 1-bit". Juga `ANALISIS_AKIBAT_KV_BERTINGKAT.md` masih berbasis arti bit-presisi yang salah.

## Pembersihan konsistensi lintas bab (user: "iya" + "sebelum edit baca bab lainnya... bab ilmu berjenjang saling berhubungan")

- **User ikut mengedit file**: menambahkan **catatan kunci di §1** yang menegaskan "64 + 192" **BUKAN** base + residual — attention punya 256 dimensi, tiap token memilih 64 (mode "1 bit") atau 256 (mode "4 bit"); "192" bukan tambahan bagi 64, dan tidak ada penggabungan/rekonstruksi. Juga memperbarui catatan §3.4 → Bab 9 menggantikan Bab 3. **Ini mengonfirmasi koreksi sebelumnya.**
- Saya baca ulang **seluruh** file (Bab 1–10) sebelum mengedit, lalu merapikan 6 titik yang masih memakai kerangka lama:
  1. **§2 langkah 4** — "Gabungkan Base+Residual → 256" dan "Zero-Padding" → diganti jadi "Tentukan Lebar Dimensi per Token": 256 untuk yang lolos seleksi, 64 untuk sisanya; tanpa padding/penggabungan/rekonstruksi.
  2. **§3.3** — catatan baru: `$x_approx = W·y` adalah mekanisme Bab 3 yang digantikan Bab 9; token 64-dim tidak direkonstruksi.
  3. **§3.5** — catatan pada biaya uncompress: pada desain final biayanya 0.
  4. **§5 langkah 5–7** — "residual 4-bit/1-bit", `Concat`, `W·y` → diganti: lebar 256 ("4 bit") vs 64 ("1 bit"); token 64-dim cukup `K_base`; **skor harus dinormalisasi dengan √d lebarnya sendiri sebelum digabung dalam satu softmax**.
  5. **§6.2** — catatan: "decode low-rank via $W^T$" tidak ada di desain final.
  6. **§6.3** — catatan: seluruh peta integrasi ditulis untuk Bab 3; bagian $W$/Oja/QR/rekonstruksi tidak diimplementasikan bila pakai Bab 9.
  7. **§4.5** — catatan diganti: slice 64 pertama sejalan Bab 9, bedanya Bab 9 memotong **setelah UnRoPE + gating SMEC**.
  8. **§7** — catatan baru: NBN (7.1) dan ECR (7.4) untuk rekonstruksi → tidak dipakai; IPN (7.2) dan CLIP (7.3) untuk seleksi → masih relevan.
- **`ANALISIS_AKIBAT_KV_BERTINGKAT.md` ditulis ulang total** (arti bit-presisi dibuang):
  - §0 definisi: "1 bit" = 64 dim, "4 bit" = 256 dim; analisis lama (SNR 6 dB, 216/864 B, dequant 22–37 GOP) tidak berlaku.
  - Risiko baru: **R1** apakah 64 dim hasil gating SMEC cukup (risiko utama); **R2** seleksi pakai 64 dim **setelah UnRoPE** → **hilang sinyal posisi** (kebalikan dari risiko lama "bias posisi"); **R3** normalisasi softmax lintas lebar; R4 kernel dua lebar; R5 prefill; R6 kriteria jangkar butuh $W$.
  - Kabar baik: **tidak ada biaya dequant**, tidak ada $W$/Oja/QR/inkonsistensi cache.
  - Pola memori: base 64 kontigu (100%), residual 192 **gather parsial 11%** (bukan streaming murni).
  - Eksperimen baru E1–E4; batas baru ditambahkan: **SMEC butuh fine-tuning offline** — tanpa itu kekhawatiran §3.1 berlaku penuh.
  - §9 "Yang sudah terjawab" mencatat 5 hal yang tidak perlu diputuskan lagi.
